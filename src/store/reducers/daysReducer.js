const initState = {
    days:[
        {
            day: 'one',
            value: 'test'
        }
    ]
}

const daysReducer = (state=initState, action)=>{
    return state
}

export default daysReducer